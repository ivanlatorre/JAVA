/*
     ear un proyecto en spring; spring.start.io


Crear los paquetes de controladores, servicios, dao, entidades, wrapper y dto

Crear Ia entidad paciente con Ios atributos ("Entity"),

Id (Long auto generado),
Nombre (String),
Apellido (String),
DNI (Integer),
Direccion (String),
(opcional: calle, altura, departamento, piso),
ultima_vicita(Date),
fecha_nacimiento(Date),
altura (double),
peso(double),
obra_social (String)

Crear el dao ("Repository")

Crear el servicio que trae todos los pacientes a travez del dao ("Service")

Crear la clase controladora con 1 metodo GET que traiga todos los pacientes. ("RestController")

 */


package Proyecto;

public class ficha_tecnica {
Id (Long auto generado)
Nombre(String),
Apellido(String),
DNI(Integer),
Direccion (String), (opcional"calle,altura,departamento")
}
